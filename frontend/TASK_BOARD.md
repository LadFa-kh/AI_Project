# Frontend Task Board

## Backlog
- [ ] Build auth pages (login/register) and Google Sign-In UX
- [ ] Build resume upload page (PDF/Word validation + UX states)
- [ ] Build skill assessment stepper (fixed level choices)
- [ ] Build evaluation result view (score + recommendations)
- [ ] Build internship match cards/list with filters/sort
- [ ] Add API client layer + typed interfaces for backend contracts
- [ ] Add loading, empty, and error states for each page
- [ ] Add frontend tests for critical user flows

## In Progress
- [ ] Establish shared context docs + AI execution workflow

## Done
- [x] Initialize frontend project scaffold
- [x] Add AI collaboration context files for frontend team

## Team Working Notes
- Frontend changes only in `frontend/`.
- Backend touchpoints tracked as contracts/checklists only (no backend code edits).
- Keep PRs small: one feature flow per PR when possible.
